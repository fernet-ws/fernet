"use strict";

// Write this JS and use babel to compile it!
// For a clean start delete all the content
window.addEventListener('DOMContentLoaded', function (event) {
  console.log('Welcome to Fernet. More info in ' + window.FERNET.URL);
});